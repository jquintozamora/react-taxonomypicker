import * as React from "react";
import * as ReactDOM from "react-dom";
import { TaxonomyPicker } from "./../../dist/React.TaxonomyPicker.js";

ReactDOM.render(
     <TaxonomyPicker />,
         document.getElementById("newsletterAppContainer")
);
